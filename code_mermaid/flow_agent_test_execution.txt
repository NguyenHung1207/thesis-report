%%{init: {
  "theme": "base",
  "sequence": {
    "mirrorActors": false,
    "showSequenceNumbers": true,
    "actorMargin": 260,
    "messageMargin": 180,
    "boxMargin": 40,
    "noteMargin": 40
  },
  "themeVariables": {
    "fontFamily": "Arial",
    "primaryTextColor": "#000000",
    "lineColor": "#000000"
  },
  "themeCSS": "
    text {
      font-size: 90px !important;
      font-weight: 900 !important;
      font-family: Arial, sans-serif !important;
    }

    .actor text,
    .labelText,
    .messageText,
    .noteText,
    .loopText,
    .messageNumber {
      font-size: 90px !important;
      font-weight: 900 !important;
    }

    .actor-line {
      stroke-width: 4px !important;
    }

    .messageLine0,
    .messageLine1 {
      stroke-width: 4px !important;
    }

    .actor rect {
      stroke-width: 3px !important;
    }

    .note {
      stroke-width: 3px !important;
    }
  "
}}%%

sequenceDiagram
    autonumber

    actor User as Người dùng
    participant System as Hệ thống
    participant W as Agent Worker
    participant AI as LLM
    participant Web as Website kiểm thử
    participant S as Kho lưu trữ

    rect rgb(246, 255, 237)
    Note over User,S: Chạy kiểm thử bằng agent

    User->>System: Nhấn Run
    System->>W: Gửi test case

    W->>AI: Suy luận hành động
    W->>Web: Thao tác kiểm thử

    alt Không nhận diện được phần tử
        W->>AI: Quan sát lại giao diện
        W->>Web: Thử cách nhận diện khác
    else Nhận diện thành công
        W->>Web: Tiếp tục thực thi
    end

    W->>S: Lưu evidence

    alt Thành công
        W-->>System: Trả kết quả
    else Thất bại
        W-->>System: Trả lỗi
    end

    System-->>User: Hiển thị báo cáo
    end